#include "main.h"
/**
 * _puts - print a string followed by new line
 * @str: string to print
 * Return: 0 alway
 */
void _puts(char *str)
{
	int i;
	for (i = 0; str[i] = '\0'; i++)
		_putchar(str[i]);
	_putchar('\0');
}
