#include "main.h"
/**
 * _abs - function that compute the absolute value of a integer
 * @n: the number
 *
 * Return: abolute value of the number
 */

int _abs(int n)
{
	int i;
	i = (abs(n));
	return (i);
}
